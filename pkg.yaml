# an example program, version: 1.0.0
version: 2

args:

pkg: "git.hpcer.dev/HPCer/CrystalMD/libcomm"
target: "comm"

dependencies:
  packages:
    github.com/google/googletest: {version: release-1.12.1, target: GTest}
